<nav class="navbar sticky-top navbar-expand-lg bg-body-secondary border-bottom border-dark border-opacity-25">
    <div class="container">
        <a class="navbar-brand ms-2 me-4" href="https://conferences.miccai.org/2023/en/" target="_new">
            <img src="{{ asset('build/images/miccai2023-logo.png') }}" height="50" alt="">
        </a>
        <a class="navbar-brand me-4" href="{{ path('app_home') }}"><img src="{{ asset('build/images/atlas-logo.png') }}" height="50" alt=""></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav nav-underline ms-auto mb-2 mb-lg-0 me-5">
                <li class="nav-item">
                    {% if route_name == "app_home" %}
                        <a class="nav-link  active" aria-current="page" href="{{ path('app_home') }}">Home</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_home') }}">Home</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if route_name == "app_dataset" %}
                        <a class="nav-link active" aria-current="page" href="{{ path('app_dataset') }}">Get the Dataset</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_dataset') }}">Get the Dataset</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if route_name == "app_challenge" %}
                        <a class="nav-link active" aria-current="page" href="{{ path('app_challenge') }}">How to Participate?</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_challenge') }}">How to Participate?</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if route_name == "app_leaderboard" %}
                        <a class="nav-link active" aria-current="page" href="{{ path('app_leaderboard') }}">Leaderboard</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_leaderboard') }}">Leaderboard</a>
                    {% endif %}
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    {% if route_name == "app_admin" %}
                        <a class="nav-link active" aria-current="page" href="{{ path('app_admin') }}">Admin</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_admin') }}">Admin</a>
                    {% endif %}
                </li>
                {% endif %}
            </ul>
            {% if app.user %}

                <ul class="primary-menu navbar-nav ml-auto mt-2 mt-md-0">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="{{asset('build/images/avatar.png')}}" class="avatar avatar-mini" alt="Avatar de {{app.user.email}}">
                            {{ app.user.email }}
                        </a>
                        <ul class="dropdown-menu text-end">
                            <li><a class="dropdown-item" href="{{ path('app_home') }}">User profile <i class="bi bi-person-lines-fill"></i> </a></li>
                            <li><a class="dropdown-item" href="{{ path('app_home') }}">Password <i class="bi bi-file-lock-fill"></i> </a></li>
                            <li><a class="dropdown-item" href="{{ path('app_home') }}">Email <i class="bi bi-envelope-at"></i> </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_logout') }}">Logout <i class="bi bi-box-arrow-right"></i></a></li>
                        </ul>
                    </li>
                </ul>
            {% else %}
                <a class="btn btn-outline-primary" href="{{ path('app_login') }}">Log in / Sign up</a>
            {% endif %}
        </div>
    </div>
</nav>